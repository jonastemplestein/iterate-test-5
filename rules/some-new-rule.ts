import { defineRule } from "@iterate-com/sdk";

export const someNewRule = defineRule({
  key: "some-new-rule-that-you-should-delete",
  prompt: "When I message around lunchtime, remind me to eat lunch",
});
